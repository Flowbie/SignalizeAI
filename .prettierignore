extension/**
node_modules/**

# Captured production HTML used by the differ load test. These are verbatim
# fixtures: reformatting them would change the bytes under test and make the
# load test assert against something no site ever served.
tests/fixtures/**

# Bundled font binaries.
fonts/**
